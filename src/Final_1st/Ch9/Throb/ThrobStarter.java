package Final_1st.Ch9.Throb;

import Final_1st.Ch9.Counter_Border.CounterFrame;

import javax.swing.*;
/*
public class ThrobStarter {
    private static final int DELAY_TIME = 1000; // 1 second delay
    public static void main(String[] args) {
        ThrobbingBall model = new ThrobbingBall();
        ThrobPanel drawingPanel = new ThrobPanel(80, model);
        ThrobFrame view = new ThrobFrame(200, drawingPanel, new ColorButton(drawingPanel));
        ThrobController controller = new ThrobController(drawingPanel, model, DELAY_TIME);
        controller.run();
    }
}
*/